## Work env preparation

### Fedora 41

Prepare development environment:

    $ pip install pipenv
    $ pipenv install

Run the server:

    $ pipenv shell
    (wedding-webpage) [user@host wedding-webpage]$ 
